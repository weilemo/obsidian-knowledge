# Project Memory

用途：维护本 Obsidian 知识库的项目级长期记忆，供 Codex、Claude Code 或其他 agent 进入本目录后优先阅读。

## 使用规则

- 进入本 vault 后，先读本文件，再按任务范围读取更具体目录里的 `memory.md`、`AGENTS.md` 或 `agent.md`。
- 只记录可复用的项目约定、稳定路径、写作风格、常见坑和已经确认的工作流；不要把一次性聊天过程塞进来。
- 追加新记忆时写日期、上下文和可执行结论；如果旧记忆失效，保留原因并标记“已过期”，不要静默覆盖。
- 用户说“记录到 Obsidian”“记到笔记”“写入教程”但没有给路径时，默认写到 `/Users/moweile/Obsidian/Knowledge`。
- 优先更新已有相关笔记，避免重复建文件；只有确实没有合适笔记时再新建。
- 笔记里只要包含数学公式，一律使用 LaTeX：块级公式优先 `$$...$$`，行内公式使用 `$...$`。

## Vault 约定

- 本目录是云端和本地 Codex 都会使用的 Obsidian vault；交付目标通常是“文件已经落到正确位置”，不是只给聊天总结。
- 涉及论文、课程、研究项目、自我管理等子区时，先进入对应子目录检查本地规则文件和已有索引。
- `Course/` 与 `course/`、`Self-management/` 与 `self-management/` 在历史任务中都出现过；写入前必须确认当前实际目录和已有文件，不要凭大小写猜测。
- 对外部网页、论文、课程资料做笔记时，优先形成可导航的 Obsidian 工作区：索引、地图、附件和双链要能闭环。
